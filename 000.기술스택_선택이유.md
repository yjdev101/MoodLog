# MoodLog 기술 스택 선택 이유

---

## 1. Java 17

- **무엇을**: 백엔드 서버 애플리케이션을 개발할 언어가 필요했습니다.
- **왜**: Spring Boot 3.x가 Java 17 이상을 요구하고, 기업에서 가장 많이 쓰는 LTS 버전입니다.
- **대안**: Java 11도 고려했지만, Spring Boot 3.x 지원을 위해 17을 선택했습니다.

---

## 2. Spring Boot 3.5

- **무엇을**: REST API 서버, 인증, DB 연동 등 백엔드 전반을 빠르게 구축해야 했습니다.
- **왜**: Security, JPA, OAuth2, Validation 등 필요한 기능이 이미 통합되어 있어 설정 없이 바로 쓸 수 있습니다. 국내외 레퍼런스가 가장 많습니다.
- **대안**: Node.js(Express)도 고려했지만, JPA와 Spring Security의 완성도가 높아 Spring Boot를 선택했습니다.

---

## 3. Spring Security + JWT

- **무엇을**: 로그인한 사용자만 자신의 감정 기록에 접근하도록 인증/인가가 필요했습니다.
- **왜**: JWT는 서버가 세션을 저장하지 않아도 되는 Stateless 구조라 확장성이 좋습니다. Spring Security와 자연스럽게 통합됩니다.
- **대안**: 세션 기반 인증도 있지만, 이후 모바일 앱 연동 가능성을 고려해 JWT를 선택했습니다.

---

## 4. Spring OAuth2 Client (Kakao / Google / Naver)

- **무엇을**: 사용자가 별도 회원가입 없이 소셜 계정으로 로그인할 수 있어야 했습니다.
- **왜**: Spring Boot가 OAuth2 클라이언트를 기본 지원해서 설정만으로 소셜 로그인을 구현할 수 있습니다.
- **대안**: 자체 회원가입만 구현할 수도 있었지만, 사용자 편의성을 위해 소셜 로그인을 추가했습니다.

---

## 5. Spring Data JPA (Hibernate)

- **무엇을**: DB에 데이터를 저장하고 조회하는 코드가 필요했습니다.
- **왜**: SQL을 직접 쓰지 않고 Java 객체로 DB를 다룰 수 있어 생산성이 높습니다. 관계 매핑(User ↔ MoodEntry 등)도 직관적으로 표현할 수 있습니다.
- **대안**: MyBatis는 SQL을 직접 제어할 수 있지만, 이 프로젝트 규모에선 JPA의 생산성이 더 적합하다고 판단했습니다.

---

## 6. MySQL

- **무엇을**: 사용자, 감정 기록, 소셜 계정 등 구조화된 데이터를 저장할 DB가 필요했습니다.
- **왜**: 데이터 간 관계(사용자 ↔ 감정 기록)가 명확해서 관계형 DB가 적합했습니다. MySQL은 레퍼런스가 많고 Docker 이미지도 안정적입니다.
- **대안**: MongoDB 같은 NoSQL도 고려했지만, 관계형 데이터 구조에는 RDB가 더 적합하다고 판단했습니다.

---

## 7. 프론트엔드 (순수 HTML / CSS / JavaScript)

- **무엇을**: 사용자에게 보여줄 화면이 필요했습니다.
- **왜**: 백엔드 REST API와 분리된 구조를 경험해보고 싶었고, 프레임워크 없이 순수 HTML/JS로 API를 호출하는 방식을 직접 구현해봤습니다.
- **대안**: React나 Vue 같은 SPA 프레임워크도 있지만, 백엔드 학습에 집중하기 위해 순수 HTML 방식을 선택했습니다.

---

## 8. Claude AI API

- **무엇을**: 사용자의 감정 일기를 분석해서 코칭 메시지를 생성하는 AI 기능이 필요했습니다.
- **왜**: 자연어 이해와 감정적 뉘앙스 파악 능력이 뛰어나고, API 형태로 간단하게 연동할 수 있습니다.
- **대안**: ChatGPT API도 고려했지만, Claude가 장문의 감정 표현을 더 섬세하게 처리한다고 판단했습니다.

---

## 9. Docker + Docker Compose

- **무엇을**: 로컬과 서버 환경을 동일하게 맞추고, app과 DB를 함께 실행할 방법이 필요했습니다.
- **왜**: "내 컴퓨터에선 되는데 서버에선 안 됨" 문제를 방지할 수 있습니다. Docker Compose로 app + MySQL을 한 번에 실행할 수 있습니다.
- **대안**: EC2에 직접 Java와 MySQL을 설치할 수도 있었지만, 환경 재현성과 관리 편의를 위해 Docker를 선택했습니다.

---

## 10. AWS EC2

- **무엇을**: 실제 외부에서 접근 가능한 서버가 필요했습니다.
- **왜**: 프리티어로 무료 사용이 가능하고, 직접 서버를 설정하고 배포하는 경험을 쌓을 수 있습니다.
- **대안**: Heroku, Railway 같은 PaaS도 있지만, 서버 직접 관리 경험을 위해 IaaS인 EC2를 선택했습니다.