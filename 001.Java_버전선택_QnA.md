# Java 버전 선택 Q&A

---

## Q1. Java 21을 쓰지 않고 17을 쓴 이유가 뭔가요?

Java 17과 21 모두 LTS 버전입니다. 21이 더 최신이지만, Spring Boot 3.x의 최소 요구사항이 Java 17이고, 프로젝트에서 21의 새 기능이 특별히 필요하지 않아 레퍼런스가 더 많은 17을 선택했습니다.

---

## Q2. LTS가 뭔가요?

**LTS(Long-Term Support)** 는 출시 후 장기간 보안 패치와 버그 수정을 공식 지원하는 버전입니다.

Java는 6개월마다 새 버전이 출시되지만, 비LTS 버전은 6개월만 지원하고 끝납니다. 실제 서비스에는 LTS 버전만 사용합니다.

| 버전 | 종류 | 지원 기간 |
|---|---|---|
| Java 17 | LTS | 2029년까지 |
| Java 18, 19, 20 | 비LTS | 6개월만 지원 |
| Java 21 | LTS | 2031년까지 |

---

## Q3. Java 21의 새 기능은 뭔가요?

### Virtual Threads (가상 스레드) — 가장 핵심
기존 Java 스레드는 OS 스레드와 1:1 매핑이라 동시에 수천 개 요청을 처리하면 메모리가 부족해집니다. Virtual Thread는 JVM이 직접 관리하는 경량 스레드라 수십만 개도 만들 수 있습니다. Spring Boot 3.2부터 설정 한 줄로 활성화할 수 있습니다.

### Record Patterns
`instanceof` 체크와 데이터 추출을 한 번에 처리하는 문법입니다.

```java
// Java 17
if (obj instanceof Point p) { ... }

// Java 21
if (obj instanceof Point(int x, int y)) { ... }
```

### Sequenced Collections
`List`, `Set`, `Map`에 `getFirst()`, `getLast()` 같은 메서드가 추가됐습니다.

---

## Q4. 대규모 트래픽의 기준은 어느 정도인가요?

트래픽 단위는 보통 **RPS(Requests Per Second, 초당 요청 수)** 를 사용합니다.

| 규모 | RPS | 예시 |
|---|---|---|
| 소규모 | ~100 RPS | 개인 프로젝트, 사내 툴 |
| 중규모 | 100 ~ 10,000 RPS | 중소 서비스 |
| 대규모 | 10,000 RPS 이상 | 쿠팡, 카카오, 네이버 |

### Java 17 스레드의 한계
기존 스레드는 1개당 약 **1MB** 메모리를 사용합니다. EC2 t2.micro (메모리 1GB) 기준으로 동시에 약 **1,000개** 스레드가 한계입니다. Virtual Thread는 1개당 수 KB 수준이라 같은 메모리로 수십~수백 배 더 많은 동시 요청을 처리할 수 있습니다.

---

## 최종 답변 정리

> "현재 프로젝트는 소규모라 초당 수십~수백 건 요청 수준이면 충분합니다. Java 17의 스레드 모델로도 감당 가능하기 때문에 Java 21의 Virtual Thread가 필요하지 않았고, Spring Boot 3.x의 최소 요구사항인 Java 17을 선택했습니다."
