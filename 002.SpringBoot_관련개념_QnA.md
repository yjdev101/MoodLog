# Spring Boot 관련 개념 Q&A

---

## Q1. REST API 서버란 무엇인가요?

**API** 는 프론트엔드(HTML/JS)와 백엔드(Spring Boot)가 데이터를 주고받는 창구입니다.

**REST** 는 그 창구를 설계하는 규칙입니다. URL로 자원을 표현하고, HTTP 메서드로 행동을 표현합니다.

| HTTP 메서드 | 의미 | 예시 |
|---|---|---|
| GET | 조회 | `GET /mood` → 감정 목록 가져오기 |
| POST | 생성 | `POST /mood` → 감정 기록 작성 |
| PUT | 수정 | `PUT /mood/1` → 1번 기록 수정 |
| DELETE | 삭제 | `DELETE /mood/1` → 1번 기록 삭제 |

즉 **REST API 서버** = 이 규칙대로 요청을 받아 처리하는 서버입니다.

---

## Q2. Node.js가 대안으로 고려되는 이유는 무엇인가요?

백엔드 서버를 만들 수 있는 기술이 Spring Boot만 있는 게 아닙니다.

| | Spring Boot | Node.js |
|---|---|---|
| 언어 | Java | JavaScript |
| 특징 | 안정성, 기업 표준 | 빠른 개발, JS 재사용 |
| 적합한 상황 | 복잡한 비즈니스 로직 | 간단한 API, 실시간 서비스 |

Node.js는 프론트엔드(JS)와 같은 언어를 쓸 수 있어서 학습 비용이 낮습니다. 그래서 백엔드 대안으로 자주 언급됩니다.

이 프로젝트는 Spring Security, OAuth2, JPA 같은 기능이 필요해서 생태계가 성숙한 Spring Boot를 선택했습니다.

---

## Q3. JPA란 무엇인가요?

**JPA(Java Persistence API)** 는 Java 객체와 DB 테이블을 연결해주는 기술입니다.

JPA 없이 DB를 쓰면 SQL을 직접 써야 합니다:

```java
// SQL 직접 작성
String sql = "SELECT * FROM mood WHERE id = ?";
```

JPA를 쓰면 Java 코드로 대체됩니다:

```java
// JPA
moodRepository.findById(id);
```

즉 **DB 작업을 Java 코드로 표현할 수 있게 해주는 도구**입니다. 반복적인 SQL 작성을 줄여줘서 생산성이 높아집니다.

---

## 최종 답변 정리

> "Spring Boot는 REST API 서버 구축에 필요한 Security, JPA, OAuth2 기능이 이미 통합되어 있습니다. Node.js도 대안이었지만, 복잡한 비즈니스 로직과 소셜 로그인 구현에는 Spring Boot의 성숙한 생태계가 더 적합하다고 판단했습니다."
