# Spring Security + JWT 관련 Q&A

---

## Q1. 세션 기반 인증 대신 JWT를 선택한 이유는 무엇인가요?

| | 세션 기반 | JWT (토큰 기반) |
|---|---|---|
| 인증 정보 저장 위치 | 서버(DB 또는 메모리) | 클라이언트(브라우저) |
| 요청마다 DB 조회 | 필요 | 불필요 |
| 트래픽 많아질수록 | 성능 저하 | 영향 없음 |

세션 기반 인증은 요청마다 서버에서 DB를 조회해야 해서 트래픽이 많아지면 성능이 저하됩니다. JWT는 서버가 상태를 저장하지 않는 **Stateless** 구조라 확장성이 좋습니다.

---

## Q2. 토큰 탈취 위험은 어떻게 대응했나요?

토큰을 localStorage에 저장하면 XSS 공격으로 탈취될 수 있습니다. 이를 최소화하기 위해 두 가지 조치를 취했습니다.

**Access Token 만료 시간 단축**
- Access Token의 만료 시간을 1시간으로 짧게 설정했습니다.
- 탈취되더라도 1시간 후에는 사용할 수 없습니다.

**Refresh Token 블랙리스트**
- Refresh Token은 만료 기간이 길어서 탈취되면 위험합니다.
- 로그아웃 시 해당 토큰을 블랙리스트에 등록해 재사용을 막았습니다.

---

## Q3. 모바일 앱 연동 가능성과 JWT는 무슨 관계인가요?

세션 기반 인증은 **쿠키** 에 세션 ID를 저장합니다. 쿠키는 브라우저 전용 기술이라 모바일 앱(Android, iOS)에서 다루기 불편합니다.

JWT는 그냥 **문자열** 이라 브라우저든 앱이든 어디서나 HTTP 헤더에 담아 보낼 수 있습니다.

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

즉 나중에 앱을 만들어도 동일한 백엔드 API를 그대로 쓸 수 있습니다.

---

## 최종 답변 정리

> "세션 기반 인증은 요청마다 서버에서 DB를 조회해야 해서 트래픽이 많아지면 성능이 저하됩니다. JWT는 서버가 상태를 저장하지 않는 Stateless 구조라 확장성이 좋고, 쿠키 없이도 동작해 모바일 앱 연동에도 유리합니다. 다만 토큰 탈취 위험이 있어 Access Token 만료 시간을 1시간으로 짧게 설정하고, Refresh Token은 로그아웃 시 블랙리스트에 등록해 재사용을 막았습니다."
