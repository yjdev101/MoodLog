# Spring Data JPA / MyBatis 관련 Q&A

---

## Q1. JPA란 무엇인가요?

**JPA(Java Persistence API)** 는 Java 객체와 DB 테이블을 연결해주는 **표준 명세(인터페이스)** 입니다.

JPA는 "규칙"이고, 실제로 동작하는 구현체가 **Hibernate** 입니다. Spring Data JPA는 Hibernate를 더 편하게 쓸 수 있도록 감싼 레이어입니다.

```
Spring Data JPA  →  JPA (표준)  →  Hibernate (실제 동작)  →  MySQL
```

---

## Q2. JPA의 핵심 개념 ORM이란?

**ORM(Object-Relational Mapping)** — Java 객체와 DB 테이블을 자동으로 매핑해줍니다.

```java
@Entity
public class MoodEntry {
    @Id
    Long id;
    String content;
    String emotion;
}
```

이 클래스 하나가 DB의 `mood_entry` 테이블 하나가 됩니다. 컬럼을 직접 만들 필요가 없습니다.

---

## Q3. JPA가 편한 이유는 무엇인가요?

메서드 이름만 규칙에 맞게 지으면 JPA가 SQL을 자동 생성합니다.

```java
// SQL 직접 작성 (JPA 없이)
"SELECT * FROM mood_entry WHERE user_id = ? ORDER BY created_at DESC"

// JPA
moodRepository.findByUserIdOrderByCreatedAtDesc(userId);
```

---

## Q4. MyBatis란 무엇인가요?

MyBatis도 Java에서 DB를 다루는 도구인데, JPA와 철학이 다릅니다. **MyBatis는 SQL을 직접 작성합니다.**

```xml
<!-- SQL을 XML 파일에 직접 작성 -->
<select id="findByUserId" resultType="MoodEntry">
    SELECT * FROM mood_entry
    WHERE user_id = #{userId}
    ORDER BY created_at DESC
</select>
```

---

## Q5. JPA vs MyBatis 비교

| | JPA | MyBatis |
|---|---|---|
| SQL 작성 | 자동 생성 | 직접 작성 |
| 생산성 | 높음 | 낮음 |
| SQL 제어권 | 낮음 | 높음 |
| 복잡한 쿼리 | 불편 | 편함 |
| 적합한 상황 | 일반적인 CRUD | 복잡한 통계/집계 쿼리 |

---

## 최종 답변 정리

> "MoodLog는 감정 기록 저장/조회 같은 단순 CRUD가 대부분입니다. 복잡한 집계 쿼리가 없어서 SQL을 직접 제어할 필요가 없었고, JPA의 자동 생성 기능만으로 충분했습니다. MyBatis는 SQL을 직접 작성해야 해서 생산성이 낮기 때문에 이 프로젝트 규모에는 JPA가 더 적합하다고 판단했습니다."
