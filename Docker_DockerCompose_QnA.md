# Docker + Docker Compose 관련 Q&A

---

## Q1. Docker란?

**"내 컴퓨터에서는 되는데 서버에서는 안 돼요"** 문제를 해결하는 도구입니다.

프로그램은 Java 버전, 환경변수, 포트 설정 등 실행 환경이 조금만 달라도 오류가 납니다. Docker는 프로그램 + 실행 환경을 **컨테이너** 단위로 묶어서 어디서 실행해도 동일하게 동작하게 합니다.

---

## Q2. 가상머신(VM)과 차이는?

| | 가상머신 | Docker |
|---|---|---|
| OS 포함 | O (수 GB) | X (수백 MB) |
| 시작 속도 | 수 분 | 수 초 |

가상머신은 컴퓨터 안에 컴퓨터를 통째로 만드는 것, Docker는 실행에 필요한 것만 묶는 것입니다.

---

## Q3. Docker Compose란?

여러 컨테이너를 한 번에 실행하고 연결해주는 도구입니다.

MoodLog는 app(Spring Boot) + db(MySQL) 두 컨테이너가 필요한데, `docker compose up` 명령어 하나로 둘 다 띄울 수 있습니다.

---

## 최종 답변 정리

> "Docker는 프로그램과 실행 환경을 컨테이너로 묶어 어디서든 동일하게 실행되게 합니다. Docker Compose는 Spring Boot와 MySQL 컨테이너를 명령어 하나로 함께 실행하고 연결해주는 도구입니다."
