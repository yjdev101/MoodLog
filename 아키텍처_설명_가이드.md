# 프로젝트 아키텍처 설명 가이드

---

## 말하는 순서

**1단계 — 한 문장 요약**
> "MoodLog는 Spring Boot 백엔드와 MySQL DB를 Docker로 컨테이너화해서 AWS EC2에 배포한 감정 일기 웹 애플리케이션입니다."

**2단계 — 그림 1 보여주며 전체 구조 설명**
> "사용자가 브라우저에서 요청하면 EC2 서버 안에서 Spring Boot 앱이 받아 처리하고, MySQL DB에 데이터를 저장합니다. 외부로는 Claude AI API와 카카오/구글/네이버 소셜 로그인을 연동했습니다."

**3단계 — 그림 2 보여주며 내부 코드 구조 설명**
> "코드 내부는 Controller → Service → Repository 3계층으로 나뉩니다. 요청이 들어오면 JWT 인증 필터를 먼저 통과하고, Controller에서 받아 Service에서 비즈니스 로직을 처리한 뒤 Repository로 DB에 접근합니다."

**4단계 — 특징적인 부분 강조**
> "일반 로그인 외에 소셜 로그인 3가지를 구현했고, 감정 일기를 작성하면 Claude AI가 코칭 메시지를 생성해주는 기능이 핵심입니다."

---

## 그림 1. 전체 시스템 구조도

```
  [사용자 브라우저]
         |
         | HTTP 요청 (port 9090)
         ▼
  ┌─────────────────────────────┐
  │         AWS EC2             │
  │                             │
  │   ┌─────────────────────┐   │
  │   │   Docker Compose    │   │
  │   │                     │   │
  │   │  ┌───────────────┐  │   │
  │   │  │  app 컨테이너  │  │   │
  │   │  │ (Spring Boot) │  │   │
  │   │  └──────┬────────┘  │   │
  │   │         │           │   │
  │   │  ┌──────▼────────┐  │   │
  │   │  │  db 컨테이너   │  │   │
  │   │  │   (MySQL)     │  │   │
  │   │  └───────────────┘  │   │
  │   └─────────────────────┘   │
  └─────────────────────────────┘
         |
         | 외부 API 호출
         ▼
  ┌──────────────────────────────┐
  │         외부 서비스           │
  │  - Claude AI API (감정 코칭)  │
  │  - Kakao OAuth2 (소셜 로그인) │
  │  - Google OAuth2             │
  │  - Naver OAuth2              │
  └──────────────────────────────┘
```

---

## 그림 2. 백엔드 레이어 구조도

```
  HTTP 요청
      |
      ▼
  ┌─────────────────────┐
  │   JWT 인증 필터      │  ← 토큰 유효성 검사
  └──────────┬──────────┘
             |
      ▼
  ┌─────────────────────┐
  │     Controller      │  ← API 엔드포인트 (URL 매핑)
  └──────────┬──────────┘
             |
      ▼
  ┌─────────────────────┐
  │      Service        │  ← 비즈니스 로직
  │                     │     └ Claude AI 호출
  └──────────┬──────────┘
             |
      ▼
  ┌─────────────────────┐
  │     Repository      │  ← DB 접근 (JPA)
  └──────────┬──────────┘
             |
      ▼
  ┌─────────────────────┐
  │       MySQL         │
  └─────────────────────┘
```

---

## 예상 질문 & 답변

### Q1. Controller, Service, Repository를 왜 3개로 나눴나요?

> 각 계층이 하나의 역할만 담당하도록 분리했습니다. Controller는 요청/응답만, Service는 비즈니스 로직만, Repository는 DB 접근만 담당합니다. 역할이 분리되면 코드 수정 시 영향 범위가 줄어들고 유지보수가 쉬워집니다.

---

### Q2. Docker를 왜 썼나요?

> 로컬 개발 환경과 EC2 서버 환경을 동일하게 맞추기 위해 사용했습니다. Docker 없이 배포하면 Java 버전이나 환경변수 차이로 오류가 날 수 있는데, Docker는 실행 환경을 통째로 묶어서 어디서든 동일하게 동작합니다.

---

### Q3. AWS EC2를 왜 선택했나요?

> 프리티어로 무료 사용이 가능하고, 직접 서버를 설정하고 배포하는 경험을 쌓고 싶었습니다. Heroku 같은 서비스는 설정이 자동화되어 있지만 EC2는 직접 관리하기 때문에 배포 과정을 깊이 이해할 수 있습니다.

---

### Q4. JWT 인증 필터가 하는 역할이 뭔가요?

> 모든 API 요청이 들어올 때 가장 먼저 실행됩니다. 요청 헤더에 담긴 JWT 토큰이 유효한지 검사하고, 유효하면 해당 사용자 정보를 Spring Security에 등록해서 이후 로직에서 사용할 수 있게 합니다. 유효하지 않으면 401 에러를 반환합니다.

---

### Q5. Claude AI는 어떻게 연동했나요?

> Spring WebFlux의 WebClient를 사용해서 HTTP로 Claude API를 호출했습니다. 사용자가 감정 일기를 저장하면 Service 계층에서 일기 내용을 Claude API에 전달하고, 반환된 코칭 메시지를 사용자에게 보여주는 방식입니다.

---

### Q6. 소셜 로그인은 어떻게 구현했나요?

> Spring Security의 OAuth2 Client를 사용했습니다. 사용자가 카카오/구글/네이버 로그인 버튼을 누르면 해당 소셜 서비스로 리다이렉트되고, 인증이 완료되면 서버로 돌아와 JWT 토큰을 발급합니다. 소셜마다 반환하는 사용자 정보 형식이 달라서 이를 통일하는 처리를 직접 구현했습니다.

---

### Q7. 데이터베이스 설계는 어떻게 했나요?

> 주요 테이블은 User(사용자), MoodEntry(감정 기록) 두 가지입니다. User 한 명이 여러 개의 MoodEntry를 가질 수 있는 1:N 관계로 설계했고, JPA의 연관관계 매핑으로 구현했습니다.
